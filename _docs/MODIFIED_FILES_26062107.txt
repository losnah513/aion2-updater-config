KINOJO MODIFIED FILES 26062107

[APPSCRIPT]
수정
- AppsScript/mvp_score_logic.gs
  · 관리자 패널 내부에만 있던 MVP 후보 계산 흐름을 buildMvpCandidates_() 공통 함수로 분리
  · getMvpAdminData_()는 새 계산식이 아니라 buildMvpCandidates_() 결과를 재사용하도록 정리
  · MVP_CONFIG는 시즌 관리, MVP_DAILY_LOG는 후보 점수 원본 역할 유지

- AppsScript/hall_of_fame.gs
  · 명예의 전당 응답에 mvpConfirmed 추가
  · 명예의 전당 응답에 mvpCandidatesTop3 추가
  · 기존 MVP_DAILY_LOG 기반 후보 계산 결과를 공개 영역용 TOP3로 병합
  · CharacterImages의 profileImageUrl을 MVP 후보 데이터에 병합
  · 확정 MVP가 있을 때는 확정 MVP를 1위 표시 기준으로 우선 배치하고 나머지 후보를 보강

[WEB]
수정
- GitHub_Pages/hall-of-fame/js/hall-render.js
  · MVP 카드 렌더링 분기 개편
  · 확정 전: 후보 1~3명 프로필 이미지만 블러 처리, 이름/순위 비공개
  · 확정 후: MVP 엠블럼 + 1위 프로필 이미지 + 1위 이름 표시
  · 확정 후 2~3위 후보는 이름만 표시
  · MVP 카드 progressive preload 대상에 mvpCandidatesTop3 이미지 추가

- GitHub_Pages/hall-of-fame/css/hall.css
  · MVP 후보 블러 이미지 레이아웃 추가
  · 확정 MVP 엠블럼/프로필 이미지/후보명 레이아웃 추가
  · 모바일 후보 이미지/확정 MVP 레이아웃 보정

- GitHub_Pages/hall-of-fame/index.html
  · hall.css 캐시값 26062107로 갱신
  · hall-render.js 캐시값 26062107로 갱신

[EXTENSION]
수정 없음

[루트]
수정 없음

[검수]
- 기존 MVP 후보 계산식 중복 생성 없음
- getMvpAdminData_()와 명예의 전당이 동일 buildMvpCandidates_() 흐름 공유
- MVP_CONFIG / MVP_DAILY_LOG 역할 유지
- hall-render.js 문법 체크 통과
- mvp_score_logic.gs 문법 체크 통과
- hall_of_fame.gs 문법 체크 통과

-- Schema-only read of operating types; synthetic rows only.
create table public.official_lookup_rate_state(provider text,paused_until timestamp with time zone,next_request_at timestamp with time zone,last_http_status integer,last_retry_after_seconds integer,consecutive_429 integer,last_session_id text,last_source text,last_error text,created_at timestamp with time zone,updated_at timestamp with time zone);
create table public.updater_session_progress_current(session_id text,requested_by_member_id bigint,started_at timestamp with time zone,session_status text,job_status text,batch_status text,control_state text,active boolean,total_count integer,completed_count integer,success_count integer,skipped_count integer,failed_count integer,retry_pending_count integer,queued_count integer,claimed_count integer,remaining_count integer,current_step integer,current_step_label text,current_character text,active_position integer,progress_payload jsonb,phases jsonb,failure_preview jsonb,latest_event jsonb,latest_event_id bigint,session_payload jsonb,job_payload jsonb,batch_payload jsonb,queue_meta jsonb,source_summary jsonb,handoff jsonb,postprocess jsonb,plaync_rate_gate jsonb,execution_source text,eta_seconds integer,batch_expires_at timestamp with time zone,worker_lease_until timestamp with time zone,updated_at timestamp with time zone,terminal_at timestamp with time zone);
create table public.kinojo_server_automation_settings(automation_key text,enabled boolean,schedule_kst jsonb,pre_block_minutes integer,post_block_minutes integer,running boolean,active_run_id text,active_session_id text,running_since timestamp with time zone,last_started_at timestamp with time zone,last_finished_at timestamp with time zone,last_status text,last_message text,updated_by text,created_at timestamp with time zone,updated_at timestamp with time zone);
create table public.lookup_session_steps(id bigint,session_id text,step_key text,step_order integer,status text,progress_current integer,progress_total integer,message text,detail jsonb,started_at timestamp with time zone,finished_at timestamp with time zone,created_at timestamp with time zone,updated_at timestamp with time zone);
create table public.updater_runtime_jobs(job_id text,session_id text,session_token_hash text,tool_name text,client_id text,requested_by_character text,requested_by_role text,requested_by_level integer,status text,current_stage text,current_character text,message text,progress_current integer,progress_total integer,eta_seconds integer,avg_ms_per_item numeric(12,2),heartbeat_interval_seconds integer,timeout_seconds integer,started_at timestamp with time zone,last_heartbeat_at timestamp with time zone,finished_at timestamp with time zone,summary jsonb,raw_payload jsonb,created_at timestamp with time zone,updated_at timestamp with time zone);

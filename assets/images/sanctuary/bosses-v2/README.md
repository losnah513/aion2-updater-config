# Sanctuary Boss Assets v2

KINOJO HOME 및 모바일 성역 카드의 보스 전경 레이어입니다.

- `*.png`: 투명 배경 마스터(1080 × 1080)
- `*.webp`: 웹 배포용 투명 WebP(1080 × 1080)
- 카드 배경은 상위 `backgrounds/` 폴더의 별도 이미지 사용

보스와 배경을 분리해 카드 밖으로 보스가 튀어나오는 인터랙션에서도 검은 사각형이나 배경 중복이 생기지 않도록 구성했습니다.
